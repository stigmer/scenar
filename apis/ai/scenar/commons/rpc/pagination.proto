syntax = "proto3";

package ai.scenar.commons.rpc;

// PageInfo contains offset-based pagination parameters.
message PageInfo {
  // Page number (1-indexed).
  int32 num = 1;
  // Maximum number of items per page.
  int32 size = 2;
}
