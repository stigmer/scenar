{
  "name": "@scenar/scripts",
  "version": "0.0.0",
  "private": true,
  "description": "Repo release/build tooling and its tests. Never published.",
  "type": "module",
  "scripts": {
    "test": "vitest run --passWithNoTests --config ../vitest.config.ts"
  }
}
